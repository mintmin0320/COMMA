import styled from 'styled-components';
import theme from '../../theme';
import { flexbox, inlineFlexbox } from '../../utils/flexbox';

export const KakaoContainer = styled.section`
  padding: 20px;
  
  .kakao-title {
    ${theme.H5};
    color: #373838;
  }
`;
