import styled from 'styled-components';
import theme from '../../theme';

export const Container = styled.section`
  // common
  padding: 30px 20px;
  overflow: hidden;

  .login-title {
    ${theme.H5};
    margin-bottom: 20px;
  }

  // common end
`;