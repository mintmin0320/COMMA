import styled from 'styled-components';
import theme from '../theme';
import { flexbox, inlineFlexbox } from '../utils/flexbox';

export const PointListContainer = styled.section`
  padding: 20px;
`;
