import React, { useEffect, useRef, useState } from 'react';

import JoinPage from '../../components/account/JoinPage';

const Join = () => (
  <React.Fragment>
    <JoinPage/>
  </React.Fragment>
);

export default Join;